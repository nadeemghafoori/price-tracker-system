DEBUG = True    # if debug is true, it will show the errors inside the browser while running the program

ADMINS = frozenset([            # it is a set that cannot be changed
    "your-email@example.com"
])